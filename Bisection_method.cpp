#include "Root.h"

using namespace std;

/**
 * bisection - uses the bisection method to find
 * root of the given function
 * @range: range of values where the function is continous on,
 * and has guaranteed root in between.
 * 
*/
float bisection(float range, stack func)
{
    
}